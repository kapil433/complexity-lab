# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Lab artifacts (rebuilt from raw + reference via `lab ingest`)
data/lab.duckdb
data/lab.duckdb.wal
data/user_lab.duckdb
data/user_lab.duckdb.wal
outputs/

# Proprietary wholesale data — never commit (raw or derived caches)
data/raw/wholesale*

# Quarto build (_freeze IS committed: it carries locally-executed results so CI
# can publish wholesale-dependent experiments without the proprietary data)
/.quarto/
/_site/
experiments/*_files/

# Streamlit
.streamlit/secrets.toml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

**/*.quarto_ipynb
